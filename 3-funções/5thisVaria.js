/*
 * O 'this' usado em uma 'function' vai sempre
 * "olhar" para o contexto onde a function foi chamada.
 * Pode-se dizer que o this em uma function pode mudar
 * dependendo de onde for chamada.
 * 
 * No entanto, se o this for usado em uma arrow function
 * (=>), essa referencia fica fixa, o this não muda mais
 * ou seja, o this usado em uma arrow function fica fixo
 * independente do contexto onde ela for chamada
*/ 